# PR Digest — dapr/dapr

> Focus on the highest-risk pull requests first, starting with PR #9974 'feat: search and vector blocks' (risk score 7) which has multiple flags including large diffs and no linked issue. Next, address PR #9719 'fix: make DeliverBulk fallthru consistent with Deliver for empty status' (risk score 6) also flagged for large diffs.

| Rank | PR | Summary | Linked issue | Risk | Flags |
|---|---|---|---|---|---|
| 1 | #9974 feat: search and vector blocks | This pull request introduces new search and vector blocks to the Dapr framework, enhancing its capabilities for handling search functionalities and vector-based operations. It includes significant additions to the codebase, including new components and API endpoints for these features. | — | 7 | many-files, large-diff, no-linked-issue |
| 2 | #9719 fix: make DeliverBulk fallthru consistent with Deliver for empty status | This pull request fixes an inconsistency in the behavior of the `DeliverBulk` function by aligning it with the `Deliver` function regarding how empty response statuses are handled. Specifically, it changes the handling of an empty status from a retry to a success, ensuring that bulk subscriptions do not unnecessarily retry messages that are considered successful by the application. | #8737 | 6 | many-files, large-diff |
| 3 | #10054 Fix/workflow save before dispatch | This pull request addresses two failure modes that can cause a workflow to become permanently stuck in a 'RUNNING' state after transient infrastructure events. It implements a fix by ensuring that the workflow's state is saved before any side effects are triggered, thereby preventing orphaned completions and allowing for proper handling of task dispatch retries. | #9738 | 3 | large-diff |
| 4 | #9855 feat(outbox): add outboxInternalTopic metadata to override internal topic name | This pull request introduces an `outboxInternalTopic` metadata field to allow users to specify a custom internal topic name for outbox-enabled state stores, bypassing the default naming convention. This change is particularly useful in environments where the default namespace cannot be configured, ensuring compatibility with various messaging systems and naming conventions. | #8933 | 3 | large-diff |
| 5 | #9893 fix(api): standardize Configuration API errors | This pull request standardizes error handling in the Configuration API by replacing legacy error patterns with structured errors from the pkg/api/errors package. It introduces a new ConfigurationError type and updates various methods to utilize these structured errors, improving consistency across the API. | #7485 | 2 | no-tests |
| 6 | #10093 perf: store raw perf reports per version and automate chart publishing | This change preserves raw performance reports in JSON format for each version, allowing for regeneration of performance charts and cross-version comparisons, while the rendered charts are published as assets in GitHub releases. The updates include modifications to the chart generation process and the addition of compressed performance reports to the repository. | — | 1 | no-linked-issue |
| 7 | #10053 test: validate pubsub CloudEvent IDs are UUIDs | This pull request adds end-to-end tests to validate that CloudEvent IDs received by the Pub/Sub subscriber are valid UUIDs, addressing a regression introduced in Dapr 1.10.0. It modifies the application code to include the new CloudEvent ID field in the response structure and updates the tests accordingly. | #5962 | 0 | — |
